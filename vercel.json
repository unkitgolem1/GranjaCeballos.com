{
  "buildCommand": "npm install && npx tailwindcss -i ./static/css/styles.css -o ./static/css/output.css && uv export --no-dev --format requirements-txt > requirements.txt",
  "outputDirectory": ".",
  "functions": {
    "api/index.py": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.py" }
  ]
}
