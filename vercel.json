{
  "buildCommand": "npx tailwindcss -i ./static/css/styles.css -o ./static/css/output.css --minify && uv export --no-dev --format requirements-txt > requirements.txt",
  "outputDirectory": ".",
  "functions": {
    "api/index.py": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.py" }
  ],
  "crons": [
    {
      "path": "/api/suscripciones/procesar",
      "schedule": "0 0 * * *"
    }
  ]
}
